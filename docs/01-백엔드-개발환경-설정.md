# 백엔드 개발 환경 설정 (Issue #89)

> **GitHub Issue**: https://github.com/Capstone-C/BE-FE/issues/89  
> **담당자**: qoweh  
> **PR**: #90 (Merged)

---

## 📋 목차
1. [기술 스택](#1-기술-스택)
2. [프로젝트 구조](#2-프로젝트-구조)
3. [주요 설정 파일](#3-주요-설정-파일)
4. [Docker 환경](#4-docker-환경)
5. [외부 API 설정](#5-외부-api-설정)
6. [보안 설정](#6-보안-설정)
7. [개발 가이드](#7-개발-가이드)
8. [예상 질문 및 답변](#8-예상-질문-및-답변)

---

## 1. 기술 스택

### 핵심 프레임워크
| 기술 | 버전 | 용도 |
|------|------|------|
| **Spring Boot** | 3.5.6 | 백엔드 프레임워크 |
| **Java** | 17 (LTS) | 프로그래밍 언어 |
| **Gradle** | 8.10 | 빌드 도구 |
| **MySQL** | 8.0 | 데이터베이스 |

### 주요 의존성
```groovy
// build.gradle 주요 의존성
dependencies {
    // Spring Boot 스타터
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.springframework.boot:spring-boot-starter-security'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
    
    // JWT 인증
    implementation 'io.jsonwebtoken:jjwt-api:0.12.6'
    
    // API 문서화
    implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.7.0'
    
    // AWS S3
    implementation 'io.awspring.cloud:spring-cloud-aws-starter-s3:3.1.1'
    
    // 개발 편의
    compileOnly 'org.projectlombok:lombok'
}
```

---

## 2. 프로젝트 구조

```
CC_BE/
├── build.gradle              # 빌드 설정
├── Dockerfile                # Docker 이미지 정의
├── gradle.properties         # Gradle 성능 최적화 설정
├── settings.gradle           # 프로젝트 이름 설정
├── gradle/
│   └── wrapper/              # Gradle Wrapper
├── mysql-init/
│   └── 01-init.sql           # DB 초기화 스크립트
└── src/
    ├── main/
    │   ├── java/com/capstone/web/
    │   │   ├── CcBeApplication.java    # 메인 클래스
    │   │   ├── auth/                   # 인증 모듈
    │   │   ├── member/                 # 회원 모듈
    │   │   ├── diary/                  # 다이어리 모듈
    │   │   ├── refrigerator/           # 냉장고 모듈
    │   │   ├── shopping/               # 쇼핑 모듈
    │   │   ├── posts/                  # 게시판 모듈
    │   │   ├── config/                 # 설정 클래스
    │   │   └── common/                 # 공통 유틸
    │   └── resources/
    │       ├── application.yml         # 애플리케이션 설정
    │       └── static/index.html       # 기본 페이지
    └── test/                           # 테스트 코드
```

---

## 3. 주요 설정 파일

### 3.1 application.yml
```yaml
spring:
  profiles:
    active: local
  
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:ccdb}
    username: ${MYSQL_USER:ccuser}
    password: ${MYSQL_PASSWORD:devpass}
    hikari:
      connection-init-sql: "SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci"
  
  jpa:
    hibernate:
      ddl-auto: update  # 개발: update, 운영: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret: ${JWT_SECRET}
  expiration-millis: ${JWT_EXPIRATION_MILLIS:3600000}

gemini:
  api-url: https://generativelanguage.googleapis.com/v1beta/models/
  api-key: ${GEMINI_API_KEY:}
  model: ${GEMINI_MODEL:gemini-2.5-flash}

naver:
  api:
    client-id: ${NAVER_CLIENT_ID:}
    client-secret: ${NAVER_CLIENT_SECRET:}

spring.cloud.aws:
  s3:
    bucket: ${AWS_S3_BUCKET:capston-cc-images}
  region:
    static: ${AWS_REGION:ap-northeast-2}
```

### 3.2 gradle.properties (빌드 최적화)
```properties
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.daemon=true
org.gradle.jvmargs=-Xmx2g -XX:+UseParallelGC
org.gradle.workers.max=4
```

---

## 4. Docker 환경

### 4.1 Dockerfile (멀티 스테이지 빌드)
```dockerfile
# Stage 1: 빌드
FROM gradle:8.10-jdk17 AS builder
WORKDIR /app
COPY . .
RUN gradle clean build -x test

# Stage 2: 실행
FROM eclipse-temurin:17-jdk
RUN apt-get update && apt-get install -y \
    tesseract-ocr \
    tesseract-ocr-kor
WORKDIR /app
COPY --from=builder /app/build/libs/cc-be.jar app.jar
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**멀티 스테이지 빌드의 장점:**
- 빌드 환경과 실행 환경 분리
- 최종 이미지 크기 최소화
- 보안 향상 (빌드 도구 제외)

### 4.2 compose.yaml
```yaml
services:
  mysql:
    image: mysql:8.0
    container_name: cc_mysql
    environment:
      MYSQL_DATABASE: ccdb
      MYSQL_USER: ccuser
      MYSQL_PASSWORD: devpass
      MYSQL_ROOT_PASSWORD: rootpass
      TZ: Asia/Seoul
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./CC_BE/mysql-init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      retries: 10

  backend:
    build:
      context: ./CC_BE
      dockerfile: Dockerfile
    container_name: cc_backend
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: local
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      JWT_SECRET: ${JWT_SECRET}
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      # ... 기타 환경변수

  frontend:
    build: ./CC_FE
    container_name: cc_frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mysql_data:
```

### 4.3 서비스 아키텍처
```
┌──────────────────────────────────────────────────────────────┐
│                    Docker Compose Network                     │
├──────────────┬────────────────────┬──────────────────────────┤
│    MySQL     │      Backend       │        Frontend          │
│  (cc_mysql)  │   (cc_backend)     │     (cc_frontend)        │
│  Port: 3306  │    Port: 8080      │       Port: 80           │
├──────────────┴────────────────────┴──────────────────────────┤
│  Volumes: mysql_data, ./uploads:/app/uploads                 │
└──────────────────────────────────────────────────────────────┘
```

---

## 5. 외부 API 설정

### 5.1 Google Gemini AI
- **용도**: 영수증 OCR, 식재료 파싱
- **모델**: gemini-2.5-flash
- **설정 클래스**: `GeminiConfig.java`

### 5.2 Naver Shopping API
- **용도**: 상품 검색
- **인증**: Client ID/Secret 헤더 방식

### 5.3 AWS S3
- **용도**: 이미지 저장
- **리전**: ap-northeast-2 (서울)

---

## 6. 보안 설정

### 6.1 SecurityConfig.java
```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(AbstractHttpConfigurer::disable)  // REST API이므로 비활성화
            .sessionManagement(session -> 
                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/v1/auth/**").permitAll()
                .requestMatchers("/api/v1/members", "/api/v1/members/").permitAll()
                .requestMatchers("/api/v1/shopping/**").permitAll()
                .requestMatchers("/swagger-ui/**", "/v3/api-docs/**").permitAll()
                .anyRequest().authenticated()
            )
            .addFilterBefore(jwtAuthFilter, UsernamePasswordAuthenticationFilter.class)
            .addFilterBefore(jwtBlacklistFilter, JwtAuthenticationFilter.class);
        
        return http.build();
    }
    
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

### 6.2 JWT 인증 흐름
```
[로그인 요청] → [AuthService] → [JWT 토큰 발급] → [클라이언트 저장]
     ↓
[API 요청 + Authorization: Bearer {token}]
     ↓
[JwtBlacklistFilter] → [블랙리스트 체크]
     ↓
[JwtAuthenticationFilter] → [토큰 검증] → [SecurityContext 설정]
     ↓
[Controller 접근]
```

---

## 7. 개발 가이드

### 7.1 로컬 개발 환경 시작
```bash
# 1. 프로젝트 클론
git clone https://github.com/Capstone-C/BE-FE.git
cd capstone

# 2. 환경변수 설정
cp .env.example .env
# .env 파일 수정 (API 키 등)

# 3. Docker Compose로 전체 시작
docker compose up -d

# 4. 또는 백엔드만 로컬 실행
cd CC_BE
./gradlew bootRun
```

### 7.2 접속 URL
| 서비스 | URL |
|--------|-----|
| 백엔드 API | http://localhost:8080 |
| Swagger UI | http://localhost:8080/swagger-ui.html |
| 프론트엔드 | http://localhost:80 |
| MySQL | localhost:3306 |

### 7.3 테스트 실행
```bash
./gradlew test
```

---

## 8. 예상 질문 및 답변

### Q1: Spring Boot 3.x를 선택한 이유는?
**A:** Spring Boot 3.x는 Java 17 이상을 요구하며, 다음과 같은 장점이 있습니다:
- **Virtual Threads 지원** (Java 21 호환 준비)
- **GraalVM Native 이미지 지원** (빠른 시작 시간)
- **Spring Security 6** (개선된 보안)
- **최신 의존성 호환** (Jackson, Hibernate 6 등)

### Q2: 왜 MySQL을 선택했나요? PostgreSQL과 비교하면?
**A:** 
- **MySQL 선택 이유**: 한국에서 가장 널리 사용, 풍부한 레퍼런스, 팀원 친숙도
- **PostgreSQL 대비**: MySQL이 단순 CRUD에서 약간 빠름
- **현재 요구사항**: 복잡한 JSON 연산이나 Full-text Search가 불필요

### Q3: Docker 멀티 스테이지 빌드의 장점은?
**A:**
1. **이미지 크기 감소**: 빌드 도구(Gradle) 제외로 50% 이상 감소
2. **보안 향상**: 빌드 환경의 민감 정보가 최종 이미지에 포함되지 않음
3. **캐시 활용**: 소스 변경 시에도 의존성 레이어 재사용

### Q4: JWT와 Session 방식의 차이점은?
**A:**
| 항목 | JWT | Session |
|------|-----|---------|
| **저장 위치** | 클라이언트 | 서버 |
| **확장성** | 수평 확장 용이 | 서버 간 세션 공유 필요 |
| **상태** | Stateless | Stateful |
| **취소** | 블랙리스트 필요 | 세션 삭제로 즉시 가능 |

우리 프로젝트는 **REST API** 기반이고 **수평 확장**을 고려하여 JWT를 선택했습니다.

### Q5: Spring Security의 Filter Chain 동작 원리는?
**A:**
```
Request → [SecurityContextPersistenceFilter]
        → [JwtBlacklistFilter] (커스텀: 블랙리스트 체크)
        → [JwtAuthenticationFilter] (커스텀: 토큰 검증)
        → [UsernamePasswordAuthenticationFilter]
        → [ExceptionTranslationFilter]
        → [FilterSecurityInterceptor]
        → Controller
```

### Q6: 환경변수를 사용하는 이유는?
**A:**
1. **12-Factor App 원칙**: 설정과 코드 분리
2. **보안**: 민감 정보(API 키, 비밀번호)가 Git에 노출되지 않음
3. **배포 유연성**: 개발/스테이징/운영 환경별 설정 변경 용이
4. **Docker 호환성**: 컨테이너 환경에서 설정 주입 표준 방식

### Q7: Gradle과 Maven의 차이점은?
**A:**
| 항목 | Gradle | Maven |
|------|--------|-------|
| **설정 파일** | Groovy/Kotlin DSL | XML |
| **빌드 속도** | 빠름 (증분 빌드, 캐싱) | 상대적으로 느림 |
| **유연성** | 높음 (프로그래밍 가능) | 제한적 |
| **학습 곡선** | 높음 | 낮음 |

우리 프로젝트는 **빠른 빌드**와 **유연한 커스터마이징**을 위해 Gradle을 선택했습니다.

### Q8: JPA의 ddl-auto 옵션별 차이점은?
**A:**
| 옵션 | 동작 | 사용 환경 |
|------|------|----------|
| `create` | 기존 테이블 삭제 후 재생성 | 테스트 |
| `create-drop` | 종료 시 테이블 삭제 | 테스트 |
| `update` | 스키마 변경만 반영 | 개발 |
| `validate` | 스키마 검증만 | 운영 |
| `none` | 아무것도 안 함 | 운영 |

---

## 📁 관련 파일 목록

| 파일 | 경로 | 용도 |
|------|------|------|
| build.gradle | `/CC_BE/build.gradle` | 빌드 설정 |
| application.yml | `/CC_BE/src/main/resources/application.yml` | 앱 설정 |
| Dockerfile | `/CC_BE/Dockerfile` | 컨테이너 이미지 |
| compose.yaml | `/compose.yaml` | 서비스 오케스트레이션 |
| SecurityConfig.java | `/CC_BE/src/.../config/SecurityConfig.java` | 보안 설정 |
| JpaAuditingConfig.java | `/CC_BE/src/.../config/JpaAuditingConfig.java` | JPA 감사 |
| WebConfig.java | `/CC_BE/src/.../config/WebConfig.java` | CORS, 인터셉터 |
