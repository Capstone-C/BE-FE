server {
    listen 80;
    server_name localhost;

    # 웹사이트의 루트 디렉토리 설정
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # 요청된 파일이 없으면 index.html을 반환 (SPA 라우팅을 위함)
        try_files $uri $uri/ /index.html;
    }
}